filters:
  and:
    # `Dev/Services/` devient faux au lot 3 ; la liste de noms selectionne seule.
    - role == "brique"
    - or:
        - file.name == "FastAPI"
        - file.name == "HTMX"
        - file.name == "Streamlit"
        - file.name == "Gradio"
        - file.name == "Dash"
views:
  - type: table
    name: Frontends web légers
    order:
      - file.name
      - pitch
      - categorie
      - langage
      - licence_type
      - maturite
      - scaling
      - tags
      - alternatives
    sort:
      - property: file.name
        direction: asc
